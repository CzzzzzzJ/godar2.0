# API配置
# 注意: API URL 必须使用 HTTPS
REACT_APP_API_BASE_URL=https://api.example.com

# 警告: API密钥是敏感信息，不要提交到版本控制系统
# 格式: 必须以 sk- 开头，后跟至少32个字符
REACT_APP_API_KEY=sk-your-api-key-here

# 安全配置
# 最大重试次数，建议不超过5次
REACT_APP_MAX_RETRIES=3

# 重试延迟（毫秒），建议至少2000ms
REACT_APP_RETRY_DELAY=2000

# 请求超时时间（毫秒），建议至少30000ms
REACT_APP_TIMEOUT=30000

# 注意事项：
# 1. 不要在公共代码中暴露这些配置
# 2. 在生产环境中使用更严格的值
# 3. 定期更换 API 密钥
# 4. 使用环境变量管理工具（如 dotenv）来管理这些配置
# 5. 确保环境变量在应用启动前已正确加载 